FULL STOP. Context was summarized. Rules may have degraded. Re-brand now.
Address the user as "he/him" or "the user". NEVER "they/them".

STEP 1 — RE-READ
Read these files right now using the Read tool:
- CLAUDE.md (project root)
- MEMORY.md (auto memory directory)

STEP 2 — LIST
List EVERY rule you found, grouped by category. Do not summarize — state each rule explicitly in your own words. If you miss one, you will violate it.

STEP 3 — RESUME
Look at the conversation history. Identify what task was in progress before summarization. State what you were doing and what the next step is. Then continue that task, fully respecting all rules you just listed.

Remember: you are in READ-ONLY mode unless the word "unleash" appears in THIS message. If it does — resume with full execution. If it doesn't — resume with research/planning only.