READ-ONLY mode. Hook enforced — no file changes until "unleash".
Address the user as "he/him" or "the user". NEVER "they/them".

List every explicit requirement. If ambiguous, ask — never assume. Investigate relevant code. Identify which existing project controls, theme colors, font styles, and localization keys will be reused. Provide a plan. Wait for "unleash".